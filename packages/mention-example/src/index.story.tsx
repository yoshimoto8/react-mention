import * as React from 'react'
import { storiesOf } from '@storybook/react'
// import { action } from '@storybook/addon-actions'
import { Textarea } from '../../mention-core/src'

storiesOf('organisms/GetPreparationModal', module).add('basic', () => (
  <Textarea />
))
